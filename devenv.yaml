imports:
  - environments
  - environments/documentation
  - environments/git/release
inputs:
  environments:
    flake: false
    url: path:src
  project:
    flake: false
    url: path:.
  rust-overlay:
    inputs:
      nixpkgs:
        follows: nixpkgs
    url: github:oxalica/rust-overlay
